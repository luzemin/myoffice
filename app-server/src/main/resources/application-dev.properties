spring.datasource.url=jdbc:mysql://192.168.10.173:3306/myoffice?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=*CBIxb0KE

mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

onlyoffice.server.url=http://localhost:8088/
onlyoffice.server.security.key=my_jwt_secret

# Auto-load dev seed data from classpath:data/data.sql on startup.
# Schema is managed manually (or via data/schema.sql); we only seed data here.
# The script clears and re-inserts seed rows, so it is idempotent.
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data/data.sql
spring.sql.init.continue-on-error=true